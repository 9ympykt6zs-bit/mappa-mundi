# Assignment: Audit Guided state-focused camera minimum zoom

Status: accepted

## Brief — Lead completes before dispatch

- Objective and user-visible outcome: identify the shared cause of state-focused Guided Learning views opening below zoom 4.7, and recommend a bounded fix that keeps complete states visible on desktop/mobile while preserving Alaska/Hawaii handling.
- Why delegation is more efficient than direct implementation: this is a focused UI/map geometry audit across several camera call paths; an independent specialist can map the fit lifecycle and responsive constraints before the Lead edits shared camera code.
- Specialist role; selected supported model/effort and reason: temporary UI / UX map specialist using `gpt-5.6-terra` at medium effort for an efficient bounded architecture audit.
- Agent/task identifier after dispatch: `/root/state_camera_audit`.
- Checkout/worktree path, branch, base revision, and pre-existing dirty files: shared checkout `/Users/ryanbarnhill/Documents/mappa-mundi`, branch `feature/evidence-driven-learning-foundation`, base `1b1d79e`; only this handoff should be dirty at dispatch.
- Relevant files, contracts, domain documentation, and concise context: inspect `AGENTS.md`, `docs/engineering-state.md`, Guided camera logic in `src/maplibre-poc.js`, `src/maplibre/maplibre-activity-runner.js`, `src/united-states-guided-political-camera.js`, related `scripts/check-*camera*.mjs`, and browser tests. The new rule is zoom >= 4.7 for views conceptually focused on one state, with complete-state padding and evidence/persistence unchanged.
- Allowed files/changes: read-only audit; fill in this handoff's Handoff section only.
- Exclusions and behavior/data that must be preserved: do not change application code, state geometry, curriculum, evidence, persistence, multi-state views, or add per-state overrides. Inspect Alaska/Hawaii rather than assuming they use the general path.
- Acceptance criteria (observable and specific): identify the calculation/path causing wide western views; enumerate all relevant state/capital teaching and identification paths; recommend a shared minimum-zoom policy with an explicit Alaska/Hawaii decision; identify focused deterministic and desktop/mobile browser tests for large western, narrow/tall, small eastern, Alaska, and Hawaii states.
- Required tests and manual/browser checks; environment constraints: read-only probes are allowed; no browser run is required for the audit. Record exact commands run and unverified areas.
- Return control to the Lead; no further delegation or unrelated edits: required.

## Handoff — specialist completes

- Result and acceptance criteria met/unmet:
  - Met. The below-4.7 western view is the managed Guided political **section** fit, not an individual-state focus. `createUnitedStatesGuidedPoliticalCameraDecision` in `src/united-states-guided-political-camera.js` selects the active section's new items (or its play items), converts capital items to their `relatedStateTargetId`, and returns a `fit` decision for sections 01–10 unless an exact authored state-set override exists. It deliberately excludes distant cumulative review from `focusTargetIds`.
  - `startUnitedStatesMemoryTrailStepIfNeeded` creates that decision (`src/maplibre-poc.js:21496`) and `startMemoryTrail` applies it once at session start (`src/maplibre-poc.js:12568`). For `fit`, `applyUnitedStatesGuidedPoliticalCamera` resolves the active state/capital targets and calls `runner.fitTargets` with desktop padding `{ top: 86, right: 82, bottom: 142, left: 82 }`, mobile section padding/offset, and only `maxZoom: 5.35` (`src/maplibre-poc.js:21378-21472`).
  - `MapLibreActivityRunner.fitTargets` combines every target's full shape bounds (or a point's 0.35-degree fallback) and passes them unchanged to `moveCamera` (`src/maplibre/maplibre-activity-runner.js:2646-2745`). `moveCamera` then delegates to MapLibre `fitBounds`, whose options contain `maxZoom` but no floor (`src/maplibre/maplibre-activity-runner.js:2117-2218`). Thus the wide Montana/Idaho/Washington/Oregon and other western groups are correctly fitted below 4.7 when their combined extent plus UI padding requires it. Existing browser coverage only asserts section 10 exceeds 3.4, confirming this is intentional current behavior rather than a later overwrite.
  - Relevant teaching/identification paths: (1) Guided states and capitals both enter through the state section: `buildUnitedStatesMemoryTrailItems` assigns capital items the paired state's `homeActivityId` and `relatedStateTargetId` (`src/united-states-memory-trail-planner.js:99-151`), while `getUnitedStatesGuidedPoliticalCameraTarget` resolves state then capital targets (`src/maplibre-poc.js:21378-21402`); (2) the exact Utah/Arizona state set uses the shared authored override in `assets/maps/data/us-states-capitals-09.json:7-18`, for both state and capital prompts; (3) ordinary standalone activity, Journey, Daily Trail, and cumulative review do not receive this Guided contract; (4) managed Guided political sessions explicitly suppress later generic practice-window fits, mobile section-quiz cameras, and small-target per-prompt focus (`src/maplibre-poc.js:11329-11563`, `13131-13218`, `15596-15631`). The camera therefore persists across state/capital target changes rather than reopening wide after a prompt transition.
  - Recommended shared fix: add a named single-state-focus policy at the shared target-camera seam in `src/maplibre/maplibre-activity-runner.js` (the `getTargetFocusCamera`/`focusTarget` path), with a `minimumZoom: 4.7` only when the request semantically contains one contiguous lower-48 state. Calculate the normal padded bounds fit first, then use 4.7 only if it still keeps those bounds inside the same desktop/mobile padding; otherwise retain the calculated fit and expose the fallback in camera trace metadata. This uses geometry and viewport math, not a state-ID override table. Do **not** add a 4.7 floor to `fitTargets` or the Guided political section fit: doing so would crop a legitimate multi-state teaching area, violating the preserved multi-state-view requirement. The Lead should also decide whether a future Guided flow needs to invoke this single-state helper; the current sections intentionally remain cohort/section views, so the shared policy has no existing sections 01–10 single-state call site.
  - Alaska/Hawaii decision: retain section 11 as `preserve-special` in `src/united-states-guided-political-camera.js:64-76`; it is intentionally unmanaged by the section-fit contract. Its authored target Learn cameras are Alaska `[-149.27965, 63.16614], 3.4310` and Hawaii `[-152.68874, 21.84516], 5.2281` in `assets/maps/data/us-states-capitals-11.json:26-63`. Do not apply the lower-48 state floor to either geography, and do not fold their disconnected bounds into a generic fit. Alaska's below-4.7 authored presentation is an explicit exception; any future Alaska change needs separate whole-region visual approval.
  - Focused validation to add after implementation: deterministic unit coverage for the policy with California or Montana (large western), Idaho (narrow/tall), Rhode Island or Delaware (small eastern), plus explicit Alaska/Hawaii bypass assertions. Extend `tests/e2e/guided-political-camera.spec.js` in both `desktop-chromium` and `mobile-chromium`: verify a true one-state state prompt and its paired capital use a settled zoom of at least 4.7 while all state bounds remain inside the computed safe frame; retain the section-10 multi-state fit assertion without imposing the floor; retain section-11 preservation. Use real state geometry rather than hard-coded per-state cameras.

- Files changed and important implementation decisions:
  - Only this handoff file. No application code, tests, data, evidence, persistence, or existing camera values were changed.

- Exact test commands, outcomes, and checks not run or blocked:
  - `node scripts/check-united-states-guided-political-camera.mjs` — passed: Guided state/capital mapping, Utah/Arizona override, section-fit selection, section-11 exception, and runtime wiring assertions.
  - `node scripts/check-daily-trail-us-states-11-alaska-learn-camera.mjs` — passed: verified the Alaska/Hawaii authored Learn cameras and post-Learn non-Learn camera.
  - Browser tests were not run, as permitted by the assignment. The existing focused browser command is `npx playwright test tests/e2e/guided-political-camera.spec.js --project=desktop-chromium --project=mobile-chromium --workers=1`; it should be run after the Lead's implementation along with any added one-state geometry/frame assertions.

- Risks, remaining issues, and proposed out-of-scope changes:
  - A minimum zoom and complete-state padding are competing constraints on a narrow mobile viewport. A shared helper must calculate fit feasibility instead of blindly replacing any lower fitted zoom with 4.7, particularly for large western shapes; otherwise it cuts off state edges behind the header/answer tray. Record a trace reason for a feasibility fallback so it is diagnosable.
  - Applying the policy to `fitTargets` globally would alter physical geography, Daily Trail, Journey, and multi-state Guided cohorts. Applying it to the managed political section fit would alter the currently intentional group camera. Neither is within this bounded fix.
  - The present section 11 behavior is special but not an authored Guided political decision; preserve it while the broader Alaska/Hawaii Reconstruction/layout work remains deferred.

- Documentation updated or needing Lead update:
  - This handoff records the audit. If the Lead adds the policy, update `docs/testing.md` with the desktop/mobile command and the exact complete-state/frame evidence. Update `docs/engineering-state.md` only if the shared camera contract actually changes.

- Integration instructions if using a separate checkout (commits/patch, dependencies):
  - Shared checkout; no commit, patch, dependency, or integration action is required. Review this documentation-only diff before implementation.

## Acceptance — Lead completes

- Reviewed result and accepted/rejected: accepted as an accurate audit. The Lead implemented prompt-level state focus while retaining the specialist's feasibility warning.
- Integration changes or follow-up corrections: Guided lower-48 teaching and visible identification prompts now fit the current state with a 7.25 zoom ceiling and UI-aware padding after the initial section context camera. Name-to-place locating retains the wider search area. Alaska/Hawaii retain authored handling. A 4.7 floor is enforced by acceptance where the complete state fits; Minnesota and Montana require lower zoom on a 390px viewport to keep their full geometry visible.
- Final validation and outcome: `check-united-states-guided-political-camera.mjs` and `check-daily-trail-us-states-11-alaska-learn-camera.mjs` passed; the full fast baseline passed 108/108; desktop/mobile Guided political browser coverage passed 18/18. Desktop and mobile Montana views were visually inspected.
- Durable docs updated: `docs/guided-political-camera.md`, `docs/engineering-state.md`, and `docs/testing.md`.
